<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_myicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_myicon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Renzo_medina_seccioncurso"
        tools:targetApi="31">
        <activity
            android:name=".SaleKpi"
            android:exported="true"
            android:label="Sale Kpi"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".BestEmployee"
            android:exported="true"
            android:label="Best Employee"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".DetailEmployee"
            android:exported="true"
            android:label="Detail Employee"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>